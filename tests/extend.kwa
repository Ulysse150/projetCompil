
var int pi;
var paire pt;
var int fact_global;

class paire {
  attribute int x;
  attribute int y;

  method void constructor(int x, int y) {
    this.x = x;
    this.y = y;
  }

  method int somme() {
    var int a;
    a = 7;
    
    return this.x + this.y  ;
    

  }



   method void test_global() {
    print(fact_global);
  }

  method void augmente(){
    this.x = this.x * 2;
    this.y = 2 * this.y;
  }
  method int test_instr_et_acces_globaux(int z) {
    var int a;
    var int n;
    var int acc;
    a = 2;
    n = 9;
    acc = 1;
    while ( n > 0 ) {
      if (n % 2 != 0) { acc = a*acc; } else {} 
      a = a*a;
      n = n/2;
    }
    
    return acc;
  }

}

class triple extends paire {
  attribute int z;

  method void constructor(int x, int y, int z) {
   
    this.x = x;
    this.y = y;
    print(4);
    this.z = z;
  }

  method int somme() {
    print(pi);
    return this.x + this.y + this.z;
  }

  method int produit() {
    return this.x * this.y * this.z;
  }
}

main {
  pi = 7891;
  fact_global = 145;
  
  pt = new triple(1, 2,3);
  pt.test_global();
  print(pt.somme());
  print(pt.test_instr_et_acces_globaux(7));
 
  
}
